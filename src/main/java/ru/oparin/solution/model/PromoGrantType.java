package ru.oparin.solution.model;

/**
 * Тип доступа, который даёт промокод.
 */
public enum PromoGrantType {
    /** Полный доступ ко всем разделам (аналог PRO + управление РК + А/Б). */
    FULL_ACCESS
}
